(#let count 10)

(#procedure Int factorial ((Int num))
    (#when (#expr num <= 1)
        (return 1)
        )
    (return (mul num (factorial (sub num 1))))
    )

(#let id (factorial count))

(#procedure Int main ()
    (print_int id)
    (return 0)
    )
